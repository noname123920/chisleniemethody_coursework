\documentclass[12pt,a4paper]{article}
\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[russian]{babel}
\usepackage{geometry}
\geometry{top=2cm,bottom=2cm,left=2.5cm,right=1.5cm}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{indentfirst}
\setlength{\parindent}{1.25cm}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{caption}
\usepackage{float}
\usepackage{listings}
\usepackage{xcolor}
\usepackage[expansion=false]{microtype}

% ====== SETUP LISTINGS ======
\lstset{
    language=Matlab,
    extendedchars=true,
    breaklines=true,
    tabsize=4,
    basicstyle=\ttfamily\footnotesize,
    keywordstyle=\color{magenta},
    commentstyle=\color{green!60!black},
    stringstyle=\color{red},
    numbers=left,
    numberstyle=\tiny\color{gray},
    frame=single,
    rulecolor=\color{black!25},
    captionpos=b
}

\begin{document}

% ========== TITLE PAGE ==========
\begin{titlepage}
    \begin{center}
        \textbf{БЮДЖЕТНОЕ УЧРЕЖДЕНИЕ ВЫСШЕГО ОБРАЗОВАНИЯ}\\
        \textbf{ХАНТЫ-МАНСИЙСКОГО АВТОНОМНОГО ОКРУГА -- ЮГРЫ}\\
        \textbf{«СУРГУТСКИЙ ГОСУДАРСТВЕННЫЙ УНИВЕРСИТЕТ»}
        
        \vspace{0.8cm}
        ПОЛИТЕХНИЧЕСКИЙ ИНСТИТУТ
        
        \vspace{0.2cm}
        Кафедра прикладной математики
        
        \vspace{3.5cm}
        \textbf{КУРСОВАЯ РАБОТА}
        
        \vspace{0.3cm}
        по дисциплине «Численные методы»
        
        \vspace{2.5cm}
        ТЕМА КУРСОВОЙ РАБОТЫ
        
        \vspace{0.5cm}
        \underline{\hspace{8cm}}\\
        \vspace{0.2cm}
        {\large \textbf{Метод конечных разностей решения линейной}} \\
        {\large \textbf{двухточечной краевой задачи для ОДУ второго порядка}}
    \end{center}
    
    \vspace{2.5cm}
    \begin{flushright}
        \begin{minipage}{0.6\textwidth}
            \raggedright
            Выполнил \\
            Студент гр. 601-\_\_ \hspace{0.8cm} \underline{\hspace{2cm}} \hspace{0.5cm} / ФИО. \\
            \hfill (подпись)
            
            \vspace{0.8cm}
            Проверил \\
            \underline{\hspace{3cm}} \hspace{0.8cm} (оценка) \\
            К.ф.-м.н, доцент \hspace{0.8cm} \underline{\hspace{2cm}} \hspace{0.5cm} / Назин А.Г. \\
            \hfill (подпись)
        \end{minipage}
    \end{flushright}
    
    \vfill
    \begin{center}
        Сургут 2026
    \end{center}
\end{titlepage}

\tableofcontents
\newpage

% ========== INTRODUCTION ==========
\section*{Введение}
\addcontentsline{toc}{section}{Введение}

Многие физические, технические и экономические процессы описываются дифференциальными уравнениями. Особый класс составляют \textbf{двухточечные краевые задачи} для обыкновенных дифференциальных уравнений (ОДУ) второго порядка. Такие задачи возникают, когда известны условия не в одной точке, а на двух концах некоторого отрезка — отсюда название «двухточечные».

Математическая постановка задачи, рассматриваемой в данной работе, имеет вид:
\begin{equation*}
    -\bigl(k(x)\,u'\bigr)' + q(x)\,u = f(x), \quad a < x < b,
\end{equation*}
где $u(x)$ — искомая функция, $k(x)$, $q(x)$, $f(x)$ — заданные непрерывные функции, причём $k(x) > 0$ на всём отрезке $[a, b]$. Для единственности решения на границах отрезка $[a, b]$ задаются дополнительные условия — первого, второго или третьего рода.

Аналитическое решение такой задачи удаётся найти лишь для простейших случаев. Для реальных задач с переменными коэффициентами приходится использовать \textbf{численные методы}. Один из самых распространённых — \textbf{метод конечных разностей} (МКР). Суть метода заключается в замене непрерывной области $[a, b]$ набором отдельных точек (узлов), а производных — приближёнными формулами через значения функции в этих точках. В результате дифференциальное уравнение превращается в систему линейных алгебраических уравнений (СЛАУ), которую можно решить на компьютере.

\textbf{Цель работы:} разработать и программно реализовать метод конечных разностей для решения линейной двухточечной краевой задачи для ОДУ второго порядка с переменными коэффициентами.

Для достижения поставленной цели решаются следующие \textbf{задачи}:
\begin{enumerate}
    \item Описать постановку линейной двухточечной краевой задачи для ОДУ второго порядка с разными граничными условиями.
    \item Указать условия разрешимости краевой задачи.
    \item Изложить метод конечных разностей (интегро-интерполяционный подход) для решения этой задачи.
    \item Реализовать в среде Octave алгоритм решения краевой задачи методом конечных разностей для одномерного стационарного уравнения теплопроводности
    \begin{equation*}
        -\frac{d}{dx}\left(k(x)\frac{du}{dx}\right) + q(x)u = f(x), \quad a < x < b,
    \end{equation*}
    где $k(x) \neq \text{const}$, $q(x) \neq \text{const}$, $f(x) \neq \text{const}$ — непрерывные функции. Требуемая точность — $10^{-4}$.
    \item Оценить погрешность численного решения по правилу Рунге.
    \item Провести вычислительные эксперименты с различными краевыми условиями.
    \item Построить графики полученного решения и график погрешности.
\end{enumerate}

\section{Реализация метода конечных разностей для задачи Дирихле}
\subsection{Численное решение}

Рассмотрим краевую задачу Дирихле для уравнения теплопроводности с переменными коэффициентами:
\begin{equation*}
    -\frac{d}{dx}\left(k(x)\frac{du}{dx}\right) + q(x)u = f(x), \quad a < x < b,
\end{equation*}
с граничными условиями:
\begin{equation*}
    u(a) = u_{\text{exact}}(a), \quad u(b) = u_{\text{exact}}(b).
\end{equation*}

Коэффициенты и правая часть выбраны следующим образом:
\begin{align*}
    k(x) &= x^2 + 1, \\
    q(x) &= x + 1, \\
    f(x) &= x^5 - 21x^4 + 23x^3 - 14x^2 + 9x - 2.
\end{align*}
Точное решение задачи имеет вид:
\begin{equation*}
    u_{\text{exact}}(x) = x^4 - 2x^3 + x^2 + 3x.
\end{equation*}

\subsection{Алгоритм метода}

Метод конечных разностей основан на интегро-интерполяционном подходе. Введём равномерную сетку:
\begin{equation*}
    x_i = a + ih, \quad i = 0, 1, \dots, N, \quad h = \frac{b-a}{N}.
\end{equation*}
Дифференциальное уравнение аппроксимируется системой:
\begin{equation*}
    -\frac{1}{h^2}\left(k_{i-1/2}u_{i-1} - (k_{i-1/2} + k_{i+1/2})u_i + k_{i+1/2}u_{i+1}\right) + q_i u_i = f_i,
\end{equation*}
где $k_{i\pm1/2} = k(x_i \pm h/2)$, $q_i = q(x_i)$, $f_i = f(x_i)$.

Для граничных условий Дирихле имеем:
\begin{equation*}
    u_0 = u_{\text{exact}}(a), \quad u_N = u_{\text{exact}}(b).
\end{equation*}
Полученная система имеет трёхдиагональную матрицу и решается методом прогонки.

\subsection{Программная реализация на Octave}

Ниже приведён код решения задачи Дирихле с автоматическим сгущением сетки до достижения точности $\varepsilon = 10^{-4}$.

\begin{lstlisting}[caption={Код №1},label={lst:dirichlet}]
function x = tridiag_solve(a, b, c, d)
    n = length(d);
    for i = 2:n
        w = a(i-1) / b(i-1);
        b(i) = b(i) - w * c(i-1);
        d(i) = d(i) - w * d(i-1);
    end
    x = zeros(n,1);
    x(n) = d(n) / b(n);
    for i = n-1:-1:1
        x(i) = (d(i) - c(i) * x(i+1)) / b(i);
    end
end

a = 0; b = 1;
k = @(x) x.^2 + 1;
q = @(x) x + 1;
f = @(x) x.^5 - 21*x.^4 + 23*x.^3 - 14*x.^2 + 9*x - 2;
u_exact = @(x) x.^4 - 2*x.^3 + x.^2 + 3*x;

tol = 1e-4;
N = 5;
err_est = tol + 1;
first = true;

while err_est > tol
    h = (b - a)/(N - 1);
    x = linspace(a, b, N)';
    xmid = (x(1:end-1) + x(2:end))/2;
    
    kmid = k(xmid);
    qv = q(x);
    fv = f(x);
    
    a_diag = zeros(N,1);
    b_diag = zeros(N,1);
    c_diag = zeros(N,1);
    d_vec = zeros(N,1);
    
    for i = 2:N-1
        a_diag(i-1) = -kmid(i-1)/h^2;
        c_diag(i)   = -kmid(i)/h^2;
        b_diag(i)   = (kmid(i-1) + kmid(i))/h^2 + qv(i);
        d_vec(i)    = fv(i);
    end
    
    b_diag(1) = 1; c_diag(1) = 0; d_vec(1) = u_exact(0);
    a_diag(N-1) = 0; b_diag(N) = 1; d_vec(N) = u_exact(1);
    
    aa = a_diag(1:N-1);
    cc = c_diag(1:N-1);
    
    U = tridiag_solve(aa, b_diag, cc, d_vec);
    
    if ~first
        U_coarse = U(1:2:end);
        err_est = max(abs(U_coarse - U_prev)) / 3;
        fprintf('N = %d, h = %.6f, err_est = %.6e\n', N, h, err_est);
        if err_est < tol
            x_final = x;
            U_final = U;
            break;
        end
    else
        first = false;
    end
    
    U_prev = U;
    N = 2*N - 1;
end
\end{lstlisting}

\subsection{Результаты сгущения сетки}

В процессе решения сетка автоматически сгущалась. Результаты приведены в таблице~\ref{tab:convergence}.

\begin{table}[H]
    \centering
    \caption{Сходимость метода при сгущении сетки}
    \label{tab:convergence}
    \begin{tabular}{ccc}
        \toprule
        $N$ & $h$ & Оценка погрешности (правило Рунге) \\
        \midrule
        9   & 0.125000 & $3.374646 \times 10^{-3}$ \\
        17  & 0.062500 & $8.679494 \times 10^{-4}$ \\
        33  & 0.031250 & $2.195086 \times 10^{-4}$ \\
        65  & 0.015625 & $5.504119 \times 10^{-5}$ \\
        \bottomrule
    \end{tabular}
\end{table}

Оценка погрешности проводилась по правилу Рунге:
\begin{equation*}
    \text{err}_{\text{est}} = \frac{\max|U_{\text{coarse}} - U_{\text{fine}}|}{2^p - 1} = \frac{\max|U_{\text{coarse}} - U_{\text{fine}}|}{3}, \quad p=2.
\end{equation*}

\subsection{Анализ точности}

После завершения итераций получено численное решение на сетке с $N = 65$ узлами. Фактическая погрешность вычисляется сравнением с точным решением:
\begin{equation*}
    \text{err}_{\text{fact}} = \max_{i} |U_i - u_{\text{exact}}(x_i)|.
\end{equation*}

\begin{table}[H]
    \centering
    \caption{Сравнение оценок погрешности}
    \label{tab:error}
    \begin{tabular}{cc}
        \toprule
        Фактическая погрешность & Оценка погрешности по Рунге \\
        \midrule
        $5.507849 \times 10^{-5}$ & $5.504119 \times 10^{-5}$ \\
        \bottomrule
    \end{tabular}
\end{table}

Оценка Рунге дала значение, близкое к фактической погрешности, что подтверждает корректность работы алгоритма и надёжность критерия остановки.

\subsection{График численного и точного решения}

На рисунке~\ref{fig:solution} представлено сравнение численного решения (красные маркеры) с точным решением (синяя сплошная линия). Видно хорошее совпадение решений на всей области $[0, 1]$.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{solution_dirichlet.png}
    \caption{Сравнение численного и точного решений для задачи Дирихле}
    \label{fig:solution}
\end{figure}

\subsection{График погрешности}

График распределения погрешности по узлам сетки показан на рисунке~\ref{fig:error}. Максимальная погрешность наблюдается в середине отрезка и не превышает $5.5 \times 10^{-5}$, что соответствует заданной точности.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{error_dirichlet.png}
    \caption{Распределение погрешности по узлам сетки}
    \label{fig:error}
\end{figure}

\end{document}